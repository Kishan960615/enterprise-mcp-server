# Contributing

1. Create a focused branch.
2. Run `uv sync --all-groups`.
3. Add tests for success and denial paths.
4. Run `make lint typecheck test`.
5. Use Conventional Commits.

Capabilities must document their permission, risk class, limits, audit fields, and connector boundary. Never commit credentials or real enterprise data.
